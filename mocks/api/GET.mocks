HTTP/1.1 200 OK
X-Custom-Header: Custom-Value
Content-Type: application/json

[
    {{#each #items}}
    {
        {{#each fields}}
        "{{key}}": "{{value}}"{{#if !@last}},{{/#if}}
        {{/each}}
    }{{#if !@last}},{{/#if}}
    {{/each}}
]