HTTP/1.1 200 OK
X-Custom-Header: Custom-Value
Content-Type: application/json

[
    {{#each items}}
    {
        {{#each fields}}
        "{{key}}": {{#if (isString type)}}"{{/if}}{{value}}{{#if (isString type)}}"{{/if}}{{#unless @last}},{{/unless}}
        {{/each}}
    }{{#unless @last}},{{/unless}}
    {{/each}}
]