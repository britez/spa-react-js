HTTP/1.1 200 OK
X-Custom-Header: Custom-Value
Content-Type: application/json

[
    {{#each items}}
    {
        {{#each fields}}
        "{{key}}": "{{value}}"{{#unless @last}},{{/unless}}
        {{/each}}
    }{{#unless @last}},{{/unless}}
    {{/each}}
]